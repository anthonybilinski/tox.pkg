--- CMakeLists.txt
+++ CMakeLists.txt
@@ -51,7 +51,7 @@
 elseif(APPLE)
 else()
   if(STATIC)
-  target_link_libraries(toxvpn cap pthread ${SODIUM_LIBRARIES})
+  target_link_libraries(toxvpn cap pthread ${SODIUM_LIBRARIES} rt)
   else(STATIC)
   target_link_libraries(toxvpn systemd cap)
   endif(STATIC)
